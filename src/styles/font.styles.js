import { css } from 'styled-components'

export const typography = css`
  /* font Sizes */
  :root {
    --h1: 3rem;
    --h2: 2.25rem;
    --h3: 1.875rem;
    --h4: 1.5rem;
    --h5: 1.125rem;
    --s-medium: 1rem;
    --s-normal: 0.875rem;
    --s-small: 0.75rem;
  }

  /* font weights */
  :root {
    --bold: 700;
    --semi: 600;
    --medium: 500;
    --regular: 400;
    --light: 300;
  }
`
